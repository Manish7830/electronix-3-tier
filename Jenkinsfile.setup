pipeline {
    agent {
        label 'electronix'
    }

    stages {
        stage('Tools Configure') {
            steps {
                echo 'Tool Installation Pipeline'
            }
        }
        stage("List Update"){
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }
}